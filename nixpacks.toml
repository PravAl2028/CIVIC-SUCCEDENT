[phases.setup]
cmds = ["rm -rf /app/node_modules/.cache || true"]

[phases.install]
cmds = ["npm ci --prefer-offline"]

[phases.build]
cmds = ["npm run build:server"]

[start]
cmd = "node dist/server-api.mjs"
